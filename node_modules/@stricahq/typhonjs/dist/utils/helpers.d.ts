import { Buffer } from "buffer";
import { LanguageView, Token } from "../types";
import { EncodedWitnesses } from "../internal-types";
/**
 * returns unique tokens with sum of amount for same tokens
 */
export declare const getUniqueTokens: (tokens: Array<Token>) => Array<Token>;
export declare const getTokenDiff: (inputToken: Array<Token>, outputTokens: Array<Token>) => Array<Token>;
export declare const sanitizeMetadata: (metadata: unknown) => unknown;
export declare const compareCanonically: (str1: string, str2: string) => number;
export declare const sortTokens: (tokens: Array<Token>) => Array<Token>;
export declare const generateScriptDataHash: (witnesses: EncodedWitnesses, languageView: LanguageView) => Buffer | undefined;
export declare const getPubKeyHashListFromNativeScript: (nativeScript: any) => Array<string>;
